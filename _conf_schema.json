{
    "token": {
        "description": "校园集市 Token (X-Sc-Od)",
        "type": "string",
        "hint": "在微信小程序版校园集市中抓包获取 X-Sc-Od 请求头的值。",
        "obvious_hint": true
    },
    "proxy": {
        "description": "HTTP 代理地址 (可选)",
        "type": "string",
        "hint": "如果服务器访问校园集市超时连不上，可在此填写网络代理（如 http://127.0.0.1:7890）。若不需要请留空。"
    },
    "interval_minutes": {
        "description": "检查频率（分钟）",
        "type": "int",
        "default": 10,
        "hint": "每隔多少分钟去检查一次新帖子"
    }
}
