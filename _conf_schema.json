{
    "proxy": {
        "description": "HTTP 代理地址 (可选)",
        "type": "string",
        "hint": "如果服务器访问校园集市超时连不上，可在此填写网络代理（如 http://127.0.0.1:7890）。若不需要请留空。"
    },
    "interval_minutes": {
        "description": "检查频率（分钟）",
        "type": "int",
        "default": 10,
        "hint": "每隔多少分钟去检查一次新帖子"
    }
}
